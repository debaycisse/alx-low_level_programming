#include <stdlib.h>

/**
 * _abs - returns absolute form of a given integer
 * @n: the parameter whose value is returned
 *
 * Description: returns the absolute form of any given integer
 * Return: the absolute form of the given integer.
 */
int _abs(int n)
{
	return (abs(n));
}
