#include <string.h>
#include "main.h"

/**
 * _strlen - returns length of string
 * @s: the parameter for the string whose length is returned
 *
 * Description: computes the length of a string
 * Return: the length of the string
 */
int _strlen(char *s)
{
	return (strlen(s));
}
