#include <stdio.h>

/**
 * main - marks the entry point of the program
 *
 * Description: This prints a single double quote symbol, foolowed by and
 * string of characters on the standard output
 * Return: Always retun 0 (succuess)
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
