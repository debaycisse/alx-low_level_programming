#include <stdio.h>
/**
 * main - Displays two phrases with commas
 *
 * Description: It displays two different phrases,
 * each is seperated with comma
 * Return: Always return 0 or success
 */
int main(void)
{
	printf("%s\n", "with proper grammar, but the outcome is a piece of art,");
	return (0);
}
